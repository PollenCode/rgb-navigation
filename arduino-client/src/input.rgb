// vc = 10? % (5? + 0?)
// vd = 10? % (5? + 1?)
// ve = 10? % (5? + 2?)
// vf = 10? % (5? + 3?)
// vg = 10? % (5? + 4?)

// out 400? - 345?

// time = counter / 5 + index * 5;

// r = time % 256;
// g = 0;
// b = 255 - (time % 256);

// time = counter ? 20 : 30;

calc = (counter % 1000) / 500

r = calc ? 255 : 0;
b = calc ? 0 : 255;
